name: Keep Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # هر ۵ دقیقه
  workflow_dispatch:        # دستی هم میشه اجرا کرد

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request
        run: |
          curl -s https://nursingshmubot.onrender.com > /dev/null
